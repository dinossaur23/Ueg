<%= form_with(model: vote, local: true) do |form| %>
  <% if vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :timestamp %>
    <%= form.datetime_select :timestamp, { id: :vote_timestamp, order: [:day, :month, :year] }, { class: 'browser-default' } %>
  </div>

  <br><div class="actions waves-effect waves-light btn-large">
    <%= form.submit %>
  </div>
<% end %>
